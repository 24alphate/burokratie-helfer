fastapi==0.111.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.6
pydantic-settings==2.7.0
python-multipart==0.0.9
pypdf==4.2.0
pdfplumber==0.11.4
reportlab==4.2.0
python-dotenv==1.0.1
openai>=1.60.0
# Anthropic — primary AI layer (question translation + Claude Vision OCR).
anthropic>=0.40.0
itsdangerous==2.2.0
pymupdf==1.24.11
# Stage 4A — OCR diagnostic for Level 4 (scanned/photo) PDFs.
# These are PYTHON wrappers; the actual OCR engine must be installed at the
# OS level. See docs/OCR_INSTALL.md for `tesseract-ocr` + `tesseract-ocr-deu`
# install instructions. The backend fails GRACEFULLY when the binary is
# missing — the user sees "OCR is not installed on this server yet."
pytesseract==0.3.13
Pillow==10.4.0
